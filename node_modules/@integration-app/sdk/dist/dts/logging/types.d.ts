export declare enum LogRecordType {
    MSG = "message",
    TEXT = "text",
    ERR = "error",
    DATA = "data",
    COMP = "compare",
    API = "api-request"
}
export interface LogRecord {
    type?: LogRecordType;
    msg?: string;
    data?: any;
}
